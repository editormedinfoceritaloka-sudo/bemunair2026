# System Status Admin

- Route: `/admin/system`; read-only server-side health checks.
- Cards show backend ping and WA Engine health with latency, last checked and retry.
- Never expose WA API key, QR, connect or message controls to browser.
